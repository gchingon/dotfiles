-- Gotham Test currently reuses the NuGotham highlight map.
-- Its palette lives in ~/.config/colors/colors.json so it can be A/B tested
-- without cloning another full theme module.

return require("themes.nugotham")
