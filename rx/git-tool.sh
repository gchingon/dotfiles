#!/bin/bash
# git-tool — Legacy wrapper (functionality moved to git.zsh functions)
# Use zsh functions instead: git-pull, git-push, git-add-commit-push, git-fetch-all

echo "git-tool.sh is deprecated."
echo "Use zsh functions directly:"
echo "  git-pull, git-push, git-add-commit-push"
echo "  git-fetch-all, git-pull-all, git-status-all"
exit 1
